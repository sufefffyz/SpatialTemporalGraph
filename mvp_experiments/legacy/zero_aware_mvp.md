# Zero-Aware Traffic Forecasting

Status: migrated active  
Current location: `mvp_experiments/active/zero_aware_mvp/`  
Compatibility alias: `zero_aware_mvp/`

This MVP predates the `mvp_experiments/active/` convention. It has been moved
into the active MVP directory, while the old top-level path remains as a symlink
for existing scripts and notes.

Registered in: `mvp_experiments/registry.yml`

## Migration Rule

Prefer the canonical `mvp_experiments/active/zero_aware_mvp/` path in new
scripts. Keep the compatibility alias until historical commands have been
refreshed.
