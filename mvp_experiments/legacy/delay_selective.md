# Delay-Selective Propagation

Status: migrated active  
Current location: `mvp_experiments/active/delay_selective/`  
Former top-level alias: `delay_selective/`

This MVP predates the `mvp_experiments/active/` convention. It has been moved
into the active MVP directory, and the old top-level symlink has been removed to
keep the repository root clean.

Registered in: `mvp_experiments/registry.yml`

## Migration Rule

Prefer the canonical `mvp_experiments/active/delay_selective/` path in new
scripts. When reproducing historical commands, replace the former top-level path
with the canonical location.
