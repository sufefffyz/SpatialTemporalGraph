# Snapshot `snap-2026-06-24-001`

Date: 2026-06-24
Owner session: `[CD-MAIN][master] LargeST2019 scalable ST`

## Purpose

Create a clean artifact root for the current master session and future conductor branches.

## Scope

- Primary experiment scope: LargeST 2019 `SD / GLA / GBA`.
- Current baseline family: `STID`, `PatchSTG`, `BiST`, `MAGE`.
- Primary research scope: scalable spatiotemporal forecasting directions, including virtual nodes, bipartite bottlenecks, patch/expert methods, and Robust-LargeST style fault robustness.

## Current Evidence

- STID, PatchSTG, and BiST have results for `SD / GLA / GBA` in remote logs.
- MAGE has completed `SD / GBA`; `GLA` was still running when last checked.
- The old virtual-node report says the broad virtual-node/bipartite idea has direct prior work, so any W2 prototype needs a narrow LargeST-scale, geography-aware, sparse-assignment claim.
- The SensorFault/Robust-LargeST search says generic sensor-fault benchmarks and LargeST Gaussian-noise robustness already exist; a defensible variant would need traffic graph-aware fault design.

## Branch Policy

- Do not open branches until user confirms the branch cards.
- Keep W1 to two active branches by default: `CD-001` experiment and `CD-002` research.
- Treat old `largest2019_virtual_node` artifacts as historical inputs, not as the active root.
