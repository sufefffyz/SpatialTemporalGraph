# CD-004 Completion Report: LargeST-LTSF Smoke And Pilot

Status: `SMOKE_RUNNING_FIRST_RESULTS_AVAILABLE`

Date: 2026-06-24

## Result Summary

The execution scripts for the LargeST input-window smoke/pilot wave are ready. The old `H=672` smoke cells were stopped after the master correction, and the active first wave now fixes `H=12` while sweeping input length.

Current remote allocation:

- `178:GPU1`: `ltsf_sd_smoke_h12_light_g1` for `STID/DLinear/CycleNet` on SD, plus `ltsf_bist_sd_smoke_h12_g1` for BiST SD smoke.
- `180:GPU0`: `ltsf_sd_smoke_h12_heavy_g0` for `TimeMixer/PatchTST` on SD.

First `H=12` smoke metrics, 1 epoch only:

| Dataset | Model | L | H | MAE | RMSE | MAPE | Wall time | Peak GPU memory |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| SD | CycleNet | 192 | 12 | 26.7775 | 43.2011 | 0.1922 | 97s | ~4.3GB |
| SD | CycleNet | 336 | 12 | 31.8701 | 49.0651 | 0.2431 | 132s | ~4.7GB |
| SD | BiST | 96 | 12 | 24.3847 | 39.8731 | 0.1789 | 121s | ~4.6GB |
| SD | PatchTST | 192 | 12 | 25.9593 | 42.6183 | 0.1882 | 203s | ~7.1GB |

These numbers are smoke-only and should not be used as accuracy claims.

BiST note: the first BiST LTSF launch exposed that `LargeST/experiments/bist/main.py` did not pass `seq_len/horizon` into the internal `MLP` base module, so the module kept the default 12-step input. A minimal fix now forwards `seq_len=args.seq_len` and `horizon=args.horizon`; the original 12-to-12 behavior is unchanged. After the fix, BiST SD `L96,H12` reached training and test.

## Added Artifacts

- `ltsf_input_window/scripts/prepare_largest_ltsf_npz.py`
- `ltsf_input_window/scripts/generate_basicts_ltsf_configs.py`
- `ltsf_input_window/scripts/run_basicts_ltsf_grid.sh`
- `ltsf_input_window/scripts/run_bist_ltsf_grid.sh`
- `ltsf_input_window/scripts/collect_ltsf_results.py`
- `ltsf_input_window/scripts/plot_input_window_curve.py`

## Launch Notes

BasicTS full `H=12` launch:

```bash
PHASE=full_h12 DATASETS=SD MODELS=STID,DLinear,CycleNet,TimeMixer,PatchTST INPUT_LENGTHS=96,192,336,672 HORIZON=12 NUM_EPOCHS=50 PATIENCE=30 GPU=0 \
bash mvp_experiments/active/largest2019_scalable_st/ltsf_input_window/scripts/run_basicts_ltsf_grid.sh
```

BiST requires cache generation first:

```bash
python mvp_experiments/active/largest2019_scalable_st/ltsf_input_window/scripts/prepare_largest_ltsf_npz.py --dataset SD --input-len 96 --horizon 12
```

Current full queue allocation:

- `178:GPU1`: `ltsf_sd_full_h12_light_g1` for `STID/DLinear/CycleNet`, and `ltsf_bist_sd_full_h12_g1` for BiST.
- `180:GPU0`: split into `ltsf_sd_full_h12_patchtst_g0` and `ltsf_sd_full_h12_timemixer_g0`, so two training processes run concurrently on the card.

Verified from logs: BasicTS uses `NUM_EPOCHS=50`, `EARLY_STOPPING_PATIENCE=30`, `output_len=12`, and `EVAL.HORIZONS=[12]`; BiST uses `max_epochs=50`, `patience=30`, `seq_len=L`, and `horizon=12`.

Queue implementation note: `run_basicts_ltsf_grid.sh` now runs only the config files generated by the current invocation instead of every file already present in the phase config directory. This avoids duplicate jobs when splitting `PatchTST` and `TimeMixer` into separate screens under the same `full_h12` phase.

## Suggested Merge Note

CD-004 has isolated smoke/pilot tooling and an active SD `H=12` smoke run. Next action is to let the remaining SD smoke cells finish, run `collect_ltsf_results.py --phase smoke_h12`, and then decide whether GBA receives only scalable models or waits for the SD pilot trend.
